# PostgreSQL Connection Details
# Copy this file to .env and fill in your details

DB_USER=yourdbuser
DB_HOST=localhost
DB_DATABASE=amazon_data_analyzer
DB_PASSWORD=your_secure_password_here
DB_PORT=5432

# Amazon Selling Partner API (SP-API) Credentials
# For reports like Brand Analytics, Sales & Traffic, and for actions like changing prices.
SP_API_CLIENT_ID=your_sp_api_client_id
SP_API_CLIENT_SECRET=your_sp_api_client_secret
SP_API_REFRESH_TOKEN=your_sp_api_refresh_token

# The Marketplace ID for SP-API reports (e.g., ATVPDKIKX0DER for US)
SP_API_MARKETPLACE_ID=ATVPDxxxxx

# *** IMPORTANT *** Your alphanumeric Seller ID (Merchant Token) from Seller Central.
# This is required for fetching your SKUs from ASINs and for price automation.
# Find it under Account Info > Merchant Token.
SP_API_SELLER_ID=A1UB2ESLTAPWSD

# --- Amazon Advertising API (Ads API) Credentials ---
# For reports like Sponsored Products Search Term and campaign management.
ADS_API_CLIENT_ID=your_ads_api_client_id
ADS_API_CLIENT_SECRET=your_ads_api_client_secret
ADS_API_REFRESH_TOKEN=your_ads_api_refresh_token

# The Profile ID for your Amazon Advertising account.
# This is REQUIRED for all Amazon Ads API V3 calls.
ADS_API_PROFILE_ID=your_amazon_ads_profile_id

# --- AWS & Amazon Marketing Stream ---
# The ARN for the Kinesis Data Firehose delivery stream. This is the destination for the real-time data.
ADS_API_FIREHOSE_ARN=your_kinesis_firehose_delivery_stream_arn_here
# The ARN for the IAM Role that gives Amazon permission to assume another role to write to your Firehose. (See Step 6.A in the guide).
ADS_API_FIREHOSE_SUBSCRIPTION_ROLE_ARN=your_iam_subscription_role_arn_here
# The ARN for the IAM Role that you use to subscribe to the stream (it grants iam:PassRole). (See Step 6.B in the guide).
ADS_API_FIREHOSE_SUBSCRIBER_ROLE_ARN=your_iam_subscriber_role_arn_here

# --- AI Model API Keys ---

# (RECOMMENDED) Provide a comma-separated list of your Google Gemini API keys.
# The system will automatically sync these with the database and rotate them.
GEMINI_API_KEYS=your_first_key,your_second_key,your_third_key

# OpenAI API Key (for "ChatGPT" option in the AI Co-Pilot)
OPENAI_API_KEY=your_openai_api_key_here
