# === Hướng dẫn Cấu hình Backend ===
# 1. Đổi tên file này từ .env.txt thành .env
# 2. Điền đầy đủ tất cả các giá trị bên dưới.

# --- Chi tiết Kết nối PostgreSQL ---
DB_USER=yourdbuser
DB_HOST=localhost
DB_DATABASE=amazon_data_analyzer
DB_PASSWORD=your_secure_db_password_here
DB_PORT=5432

# --- Amazon Selling Partner API (SP-API) Credentials ---
# Sử dụng cho các báo cáo như Brand Analytics, Sales & Traffic, Search Query Performance
SP_API_CLIENT_ID=your_sp_api_client_id
SP_API_CLIENT_SECRET=your_sp_api_client_secret
SP_API_REFRESH_TOKEN=your_sp_api_refresh_token
SP_API_MARKETPLACE_ID=ATVPDKIKX0DER # Ví dụ: ATVPDKIKX0DER cho thị trường Mỹ
SP_API_ASIN_LIST="B123456789 B987654321" # Danh sách ASIN cách nhau bởi dấu cách

# --- Amazon Advertising API (Ads API) Credentials ---
# Sử dụng cho các báo cáo như Sponsored Products Search Term và quản lý chiến dịch
ADS_API_CLIENT_ID=your_ads_api_client_id
ADS_API_CLIENT_SECRET=your_ads_api_client_secret
ADS_API_REFRESH_TOKEN=your_ads_api_refresh_token
ADS_API_PROFILE_ID=your_amazon_ads_profile_id # BẮT BUỘC cho tất cả các lệnh gọi Ads API

# --- AWS & Amazon Marketing Stream ---
# Các giá trị ARN lấy từ tài khoản AWS của bạn
ADS_API_FIREHOSE_ARN=your_kinesis_firehose_delivery_stream_arn_here
ADS_API_FIREHOSE_SUBSCRIPTION_ROLE_ARN=your_iam_subscription_role_arn_here
ADS_API_FIREHOSE_SUBSCRIBER_ROLE_ARN=your_iam_subscriber_role_arn_here

# --- Khóa Bí mật cho Endpoint Nhận Dữ liệu Stream ---
# Tự tạo một chuỗi ký tự ngẫu nhiên, dài, và phức tạp.
# Đây là "chìa khóa" để xác thực các yêu cầu từ AWS Lambda đến backend của bạn.
STREAM_INGEST_SECRET_KEY=your_super_long_and_secret_random_string_here

# --- AI Model API Keys ---
# Google Gemini API Key
GEMINI_API_KEY=your_gemini_api_key_here
